// Base config for the PacketTunnelExtension (NE) target (Release) — see
// App-Base-Debug.xcconfig for why this indirection exists.
#include "../../Pods/Target Support Files/Pods-PacketTunnelExtension/Pods-PacketTunnelExtension.release.xcconfig"
#include "brand-active.xcconfig"
