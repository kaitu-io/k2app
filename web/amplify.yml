version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - rm -rf .next/cache/fetch-cache  # clear stale CDN manifest cache between builds
            - yarn install
        build:
          commands:
            # Note: Payload migrations skipped - using push: true in db config for auto schema sync
            # If you need migrations, add .ts extensions to imports in payload.config.ts
            - echo "Building Next.js application..."
            - yarn run build
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - .next/cache/**/*
          - node_modules/**/*
    appRoot: web
