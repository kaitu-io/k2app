# Tracked on purpose: amplify.yml preBuild appends the real console env vars
# at build time (standalone output does not forward them to the SSR Lambda).
# Do not put values here — they would be dead; see web/CLAUDE.md Environment.
