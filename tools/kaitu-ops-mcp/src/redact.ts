/**
 * Stub — implementation pending GREEN phase.
 * This file intentionally returns the input unchanged so tests fail (not error).
 */

/**
 * Redacts sensitive patterns from stdout text before returning to Claude.
 */
export function redactStdout(text: string): string {
  // RED: stub returns input unchanged — tests will fail
  return text
}
